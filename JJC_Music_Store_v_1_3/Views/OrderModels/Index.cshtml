@model IEnumerable<JJC_Music_Store_v_1_3.Models.Entity.OrderModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Price)
        </th>
      
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Price)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OrderModelID }) |
            @Html.ActionLink("Details", "Details", new { id=item.OrderModelID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OrderModelID })
        </td>
    </tr>
}

</table>
